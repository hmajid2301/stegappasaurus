import React, { Component } from 'react';
import { View, Text } from 'react-native';

import styles from '../components/Common/colors';


export default class Encoding extends Component {
  render() {
    return (
      <View style={styles.showCanvas}>
        <Text> Encoding </Text>
      </View>
    );
  }
}
